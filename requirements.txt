# GameDev-Edu Requirements
# Python 3.10+

# Game Runtime
# pygame>=2.5.0  # Falls pygame nicht funktioniert, verwende pygame-ce:
pygame-ce>=2.5.0  # Alternative für Python 3.14+

# Sprite Organization (optional, aber empfohlen)
Pillow>=10.0.0  # Für Bildgrößen-Prüfung beim Sortieren

# Editor GUI
PySide6>=6.5.0

# LSP (Language Server Protocol) für Code-Editor
python-lsp-server>=1.9.0  # LSP-Server für Python

# Bildverarbeitung (iCCP-Profil-Korrektur)
# Pillow wird bereits oben verwendet - keine zusätzliche Abhängigkeit nötig
# Optional: Wand>=0.6.13 für ImageMagick-Fallback (benötigt ImageMagick als System-Bibliothek)
